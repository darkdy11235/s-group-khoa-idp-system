import { Repository } from 'typeorm';
import { Role } from '../entities/role.entity';

// The RoleRepository is a simple class that extends the Repository class from TypeORM. This class is used to perform CRUD operations on the Role entity.
