// index.ts of guards folder:
import { AuthGuard } from './auth.guard';
import { PermissionsGuard } from './permissions.guard';
import { RolesGuard } from './roles.guard';

export { AuthGuard, PermissionsGuard, RolesGuard };
